import 'package:flutter/material.dart';

class CreateTextPost extends StatelessWidget {
  const CreateTextPost({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Create text post'),
      ),
    );
  }
}
