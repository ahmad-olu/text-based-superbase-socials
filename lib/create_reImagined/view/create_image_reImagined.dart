import 'package:flutter/material.dart';

class CreateImagePost extends StatelessWidget {
  const CreateImagePost({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Create image post'),
      ),
    );
  }
}
